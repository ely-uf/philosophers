#ifndef __PHILOSOPHER_STATE
# define __PHILOSOPHER_STATE

# include "philosopher.h"

int		philosopher_eat(t_philosopher *phil);
int		philosopher_rest(t_philosopher *phil);
int		philosopher_think(t_philosopher *phil);

#endif
